<Namespace Name="MonoTouch.GLKit">
  <Docs>
    <summary>GLKit, a framework to simplify configuring and using OpenGL.</summary>
    <remarks>

      <para>

	The main goal of GLKit is to simplify many repetitive and
	error prone tasks that developers face when building OpenGL
	code.  It supports: texture loading, view and view controller
	integration of OpenGL content, OpenGLES effects and a OpenGLES
	1.1 effect compatibility kit for OpenGLES 2.0 apps.

      </para>

      <para>

	GLKit offers a convenient way of loading images as GL textures
	-- both synchronously and asynchrously.  The <see
	cref="T:MonoTouch.GLKit.GLTextureLoader"/> can load textures
	and cube maps from <see
	cref="T:MonoTOuch.CoreGraphics.CGImage"/> objects, in-memory
	binary data, files and arbitrary <see
	cref="T:MonoTouch.Foundation.NSUrl"/> locations.  The textures
	are returned as <see cref="T:MonoTouch.GLKit.GLKTextureInfo"/>
	objects.

      </para>

      <para>

	It is now very easy to blend OpenGL content with Views and
	ViewControllers using <see cref="T:MonoTouch.GLKit.GLKView"/>
	and <see cref="T:MonoTouch.GLKit.GLKViewController"/>.

      </para>

      <para>

	The <see cref="T:MonoTouch.GLKit.GLKBaseEffect"/> class
	contains the most commonly used OpenGLES 1.1 effects for use
	in 2.0 applications.  These include lightning, materials,
	multi-texturing, fog, constant color and transformations.

      </para>

      <para>

	Other effects part of GLKit include <see
	cref="T:MonoTouch.GLKit.GLKSkyboxEffect"/> and <see
	cref="T:MonoTouch.GLKit.GLKReflectionMapEffect"/>.

      </para>

    </remarks>
  </Docs>
</Namespace>
