<Namespace Name="VideoSubscriberAccount">
  <Docs>
    <summary>The VideoSubscriberAccount namespace supports behind-the-scenes authentication to a known provider.</summary>
    <remarks>
      <para>The VideoSubscriberAccount namespace allows apps to authenticate with a service provider without necessarily involving the user. Once a user has authenticated with a streaming video service in one app, this framework allows other apps to re-use those credentials.  </para>
    </remarks>
  </Docs>
</Namespace>